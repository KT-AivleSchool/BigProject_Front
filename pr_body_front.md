## 📌 개요
PR #53 리뷰 피드백 조치 완료 및 동적 공청회 UI, PDF/HWPX 보고서 지도 QR코드 연동 강화를 위한 Pull Request입니다.

---

## 🛠️ 주요 피드백 조치 및 기능 반영 내역

### 1. 프런트엔드 리뷰 피드백 조치
- **환경변수 적용**: `http://localhost:8000` 하드코딩 주소를 `process.env.NEXT_PUBLIC_API_URL` 환경변수로 일괄 전환
- **Mock 대화 폴백 정돈**: 백엔드 통신 실패 시 가짜 대화로 치명적 오해를 유발하던 Mock 대화 폴백 로직을 제거하고 명확한 에러 안내 메시지 표출
- **이모지/이모티콘 UI 정리**: 화면 UI 내 어색한 이모지 문자를 정돈하여 공문서 표준 스타일에 맞춘 깔끔한 UI 유지

### 2. 동적 공청회 UI 및 리포트 연동
- **동적 공청회 3단계 위자드 (`dynamic-hearing`)**:
  - Step 1: 안건 주제, 목적, 조례 설정 입력
  - Step 2: AI 도출 이해관계자 카드 승인 및 선택 (HITL)
  - Step 3: 실시간 채팅 스트리밍, 수용도 평가 및 시나리오 보고서 렌더링
- **공청회 심의 결과 보고서 (`hearing-pdf`)**:
  - `qrcode.react` 라이브러리의 `<QRCodeSVG>` 컴포넌트를 이용해 후보지 위경도 좌표 기반 **카카오지도 및 네이버지도 위치 핀 QR코드 렌더링**
  - 클라이언트 PDF 인쇄(`window.print()`) 및 백엔드 HWPX 공문서 바이너리 다운로드 버튼 연동

---

## 🧪 검증 결과
- 프런트엔드 빌드 및 컴포넌트 JSX 구문 유효성 통과
- `window.print()` PDF 인쇄 시 카카오지도 / 네이버지도 QR코드 정상 출력 확인
- 백엔드 `/api/v1/report/download/hwpx` HWPX 한글 공문서 다운로드 연동 확인
